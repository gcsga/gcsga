// import { SYSTEM_NAME } from "@module/data/constants.ts"
// import { CombatGURPS } from "./index.ts"
// import type { TokenDocumentGURPS } from "@scene/index.ts"
//
// class CombatantGURPS<
// 	TParent extends CombatGURPS | null = CombatGURPS | null,
// 	TTokenDocument extends TokenDocumentGURPS | null = TokenDocumentGURPS | null,
// > extends Combatant<TParent, TTokenDocument> {
// 	/** Has this document completed `DataModel` initialization? */
// 	declare initialized: boolean
// }
//
// interface CombatantGURPS<
// 	TParent extends CombatGURPS | null = CombatGURPS | null,
// 	TTokenDocument extends TokenDocumentGURPS | null = TokenDocumentGURPS | null,
// > extends Combatant<TParent, TTokenDocument> {
// 	flags: CombatantFlags
// }
//
// interface CombatantFlags extends DocumentFlags {
// 	[SYSTEM_NAME]: {}
// }
//
// export { CombatantGURPS }
// export type { CombatantFlags }
