<select name="{{path}}.qualifier.compare">
	{{#select this.qualifier.compare}}
		{{#each config.select.numeric-criteria_strict as |v k|}}
			<option value="{{k}}">{{v}}</option>
		{{/each}}
	{{/select}}
</select>
<div class="field">
	<input type="text" name="{{path}}.qualifier.qualifier" value="{{this.qualifier.qualifier}}" data-dtype="Number" />
</div>
